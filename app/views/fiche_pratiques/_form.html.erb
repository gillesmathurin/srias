<%= form_for(@fiche_pratique) do |f| %>
  <% if @fiche_pratique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fiche_pratique.errors.count, "error") %> prohibited this fiche_pratique from being saved:</h2>

      <ul>
      <% @fiche_pratique.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :external_link %><br />
    <%= f.text_field :external_link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
