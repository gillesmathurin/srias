<%= form_for(@fiche_pratique) do |f| %>
  <% if @fiche_pratique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fiche_pratique.errors.count, "error") %> prohibited this fiche_pratique from being saved:</h2>

      <ul>
      <% @fiche_pratique.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Titre" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content, "Contenu" %><br />
    <%= f.text_area :content, :class => "tinymce", :cols => "90"  %>
    <%= tinymce :theme => "modern" %>
  </div>
  <div class="field">
    <%= f.label :external_link, "Lien" %><br />
    <%= f.text_field :external_link, placeholder: 'http://lien-externe.com' %>
  </div>
  <div class="field">
    <%= f.label :Fichier, "Fichier joint" %><br>
    <%= f.file_field :fichier %>
  </div>
  <div class="actions">
    <%= f.submit "Enregistrer", class: 'btn btn-success' %>
  </div>
<% end %>
