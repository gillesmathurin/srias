<div class="span-16">
	<%= render :partial => "shared/flash_messages" %>
	
	<h1 class="section-title">Listing newsletters</h1>

	<table>
	  <tr>
	    <th>Titre</th>
		<th>Envoyé le </th>
	  </tr>

	<% @newsletters.each do |newsletter| %>
	  <tr>
	    <td><%= newsletter.titre %></td>
		<td><%= newsletter.delivered_at %></td>
	    <td><%= link_to 'Voir', newsletter, :class => "button small" %></td>
		<td><%= link_to "Envoyer", deliver_newsletter_path(newsletter), :class => "button small" %></td>
	    <td><%= link_to 'Modifier', edit_newsletter_path(newsletter), :class => "button small" %></td>
	    <td><%= link_to 'Supprimer', newsletter, :data => {:confirm => 'Are you sure?'},
	     :method => :delete, :class => "button small" %></td>
	    <% if current_user.login == "gillesmath" %>
  	    <td><%= link_to "Envoi-Test", deliver_test_newsletter_path(newsletter), :class => "button small" %></td>
  	  <% end %>
	  </tr>
	<% end %>
	</table>

	<br />

	<%= link_to 'Créer newsletter', new_newsletter_path, :class => "button small midmedium" %>
</div>