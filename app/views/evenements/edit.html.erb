<h1>Editing evenement</h1>

<% form_for(@evenement) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @evenement %> |
<%= link_to 'Back', evenements_path %>