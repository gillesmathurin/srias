<div class="span-16">
	<h1>Listing missions</h1>

	<table>
	  <tr>
	    <th>Nom</th>
	    <th>Description</th>
	  </tr>

	<% @missions.each do |mission| %>
	  <tr>
	    <td><%=h mission.nom %></td>
	    <td><%=h mission.description %></td>
	    <td><%= link_to 'Voir', mission %></td>
	    <td><%= link_to 'Modifier', edit_mission_path(mission) %></td>
	    <td><%= link_to 'Supprimer', mission, :confirm => 'En êtes vous sur ?', :method => :delete %></td>
	  </tr>
	<% end %>
	</table>

	<br />

	<%= link_to 'Nouvelle mission', new_mission_path %>
</div>
<!-- Right Sidebar -->
<div class="span-4 last">
	<div class="pubs">
		right col
	</div>
</div>
