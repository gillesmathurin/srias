<%= form_for(@information) do |f| %>
  <%= render :partial => 'shared/form_errors', :locals => {:object => @information} %>

  <div class="field">
    <%= f.label :actu_category_id, "Categorie" %><br>
    <%= f.select :actu_category_id, ActuCategory.all.collect{|m| [m.nom, m.id]}, {:include_blank => true} %>
  </div>

  <div class="field">
    <%= f.label :title, "Titre" %><br/>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content, "Contenu" %><br/>
    <%= f.text_area :content, :class => "tinymce", :cols => "90" %>
    <%= tinymce :theme => "modern" %>
  </div>
  <div class="field">
    <%= f.label :information_file, "Fichier joint" %><br/>
    <%= f.file_field :information_file %>
  </div>
  <div class="field">
    <%= f.check_box :in_right_sidebar %>
    <%= f.label :in_right_sidebar, "Afficher dans la colonne de droite ?" %>
    <p>En cochant cette case cette actualité sera visible tout le temps dans la colonne de droite 'Actualités'</p>
  </div>
  <div class="field">
    <%= f.check_box :published %>
    <%= f.label :published, "Publiez cette actualité" %>
    <p>En cochant cette case, cette actualité sera visible sur la page 'Actualité', dans sa catégorie. Décochez la si vous ne souhaitez plus que les visiteurs lisent cette information.</p>
  </div>
  <div class="actions">
    <p><%= f.submit "Enregistrer", class: 'btn' %></p>
  </div>
<% end %>
