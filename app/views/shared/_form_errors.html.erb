	  	<% if object.errors.any? %>
	  		<div id="error_explanation">
	  			<h2><% pluralize(object.errors.count, "erreur") %> empêchèrent ce formulaire d'être enregistré.</h2>
	  			<ul>
	  				<% object.errors.full_messages.each do |msg| %>
	  					<li><%= msg %></li>
	  				<% end %>
	  			</ul>
	  		</div>
	  	<% end %>